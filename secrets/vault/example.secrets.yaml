# vault/example.secrets.yaml — PLAINTEXT fixture, fictional demo values only.
#
# This is the pre-encryption source. Encrypt it in place with:
#   sops -e -i vault/example.secrets.yaml
# (sops renames nothing automatically — by convention in this repo the
# encrypted result is committed as vault/example.secrets.enc.yaml; see
# vault/example.secrets.enc.yaml.sample for what that output looks like.)
#
# None of the values below are real credentials — do not reuse them anywhere.
services:
  demo_api:
    api_key: "sk_demo_FICTIONAL00000000000000000000"
  demo_db:
    password: "correct-horse-battery-staple-DEMO-ONLY"
  demo_signing:
    cert_p12_base64: "REPLACE_WITH_BASE64_OF_YOUR_OWN_CERT=="
