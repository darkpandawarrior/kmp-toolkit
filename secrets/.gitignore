# Real/private age identities — never commit these.
*.age
*.agekey
key.txt
keys.txt
.sops-age-key*
/keys/

# Materialized (decrypted) secrets, if this vault is ever decrypted locally.
out/
secrets/live/
