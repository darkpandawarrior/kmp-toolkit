<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Declared here rather than in each consuming app so shareImage() works with no host-app
          manifest edit. The authority is `.kmpshare`, not the conventional `.fileprovider`: apps
          in this fleet already declare that one, and two providers sharing an authority is a
          manifest-merger failure at build time.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.kmpshare"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/kmp_share_paths" />
        </provider>
    </application>
</manifest>
